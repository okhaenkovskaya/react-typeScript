"use strict";
const isActive = true;
const heading = 'My Heading';
const year = 20;
const sizes = [3, 7, 10, 15];
function printSizes(obj) {
    return obj;
}
printSizes({ size: "10" });
console.log('modulo js 445');
